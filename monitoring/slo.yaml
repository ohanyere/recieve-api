apiVersion: sre.platform.ohanyere.io/v1alpha1
kind: ServiceLevelObjective
metadata:
  name: recieve-api-availability
  labels:
    app.kubernetes.io/name: recieve-api
    finops.ohanyere.io/cost-center: payments
spec:
  service: recieve-api
  owner: Payments
  objective: 99.9
  window: 30d
  indicator:
    type: availability
    goodEvents: sum(rate(http_requests_total{service="recieve-api",status!~"5.."}[5m]))
    totalEvents: sum(rate(http_requests_total{service="recieve-api"}[5m]))
  alerting:
    burnRate:
      enabled: true
